# Cfx Server (Windows) on Wine, for Pterodactyl

Reason for this image is that Linux builds of FXServer for enhanced are currently broken.
I'm not making this to keep it active, but for as long as Linux builds are broken, I'll have this version
so we can run the Windows version from Linux servers running Pterodactyl.

Use at your own risk.
